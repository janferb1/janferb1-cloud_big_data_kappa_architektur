apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: kappa-iot
spec:
  selector:
    pod: kafka-pod
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
      protocol: TCP
  type: ClusterIP
